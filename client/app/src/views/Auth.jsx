// standard library
import React from "react"
// external libraries

// internal libraries
import "../style/auth.css"

const Auth = () => {
	

	return (
		<div className={`auth`}>
			Auth (login/logout) screen
		</div>
	)
}

export default Auth
